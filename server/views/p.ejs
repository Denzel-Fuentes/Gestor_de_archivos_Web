
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">;
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-aFq/bzH65dt+w6FI2ooMVUpc+21e0SRygnTpmBvdBgSdnuTN7QbdgL+OapgHtvPp" crossorigin="anonymous">

    <title>Document</title>
</head>
<body>
    <div class="container">

        <div class="page-header text-center"></div>
            <h1><span class="fa fa-lock"></span> GESTOR DE ARCHIVOS</h1>
            <a href="/logout" class="btn btn-default btn-sm"> Logout</a>
        <div class="row">
            <div class="col-sm-6">
                <div class="well h3 text-danger"><span class="fa fa-google-plus"></span> </div>
    
                <p>
                    
                    <strong>email</strong>:<%= user.email %> <br>
                    <%if (user.name) {%>
                        <strong>name</strong>:<%= user.name%> <br>
                      <% } %>
                      <%if (user.pic) {%>
                        <strong>pic</strong>:<img src="<%= user.pic %>" width="100">
                      <% } %>
                    
    
    
                </p>
    
            </div>
            <div class="col-sm-6">
                  <div>
                    <h1>Carga de Archivos</h1>
                    <form action="/Archivo" method="post" enctype="multipart/form-data">
                        <input type="file" name="file"  multiple="false" id="leftimage">
                        <input type="text" name="text"  multiple="false" id="lef">

                        <br>
                        <input type="submit" value="Confirmar">
                    </form>
                    
                  </div>
                  <div>
                    <h1>Functions</h1>
                    <input type="button" value="MERGE" id="mergeButton" onclick="doMesh()">
                  </div>
            </div>
        </div>
        <div class="row">
            <ul>
                <% for(var i=0; i<files.length; i++) { %>
                    <%  
                        const fechaActual = new Date(Date.parse(files[i].date));
                        const anio = fechaActual.getUTCFullYear();
                        const mes = ('0' + (fechaActual.getUTCMonth() + 1)).slice(-2);
                        const dia = ('0' + fechaActual.getUTCDate()).slice(-2);
                        const fecha = `${anio}-${mes}-${dia}`; 
                    %>
                  <li>Nombre: <%= files[i].filename %>, tamaño: <%= files[i].size %>KB , date <%= fecha %></li>
                <% } %>
            </ul> 
        </div>

    </div>
</body>
</html>